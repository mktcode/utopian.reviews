var config = {
    app: {
        port: 5555
    },
    oauth: {
        client_id: "",
        redirect_uri: "http://localhost:5555/auth"
    },
    session: {
        secret: "changeme",
        mod_session: "changeme"
    }
};

module.exports = config;